# Changelog

## 1.0.0

- Canonical PowerChain (PWRC) Token-2022 monetary policy.
- Devnet-first and guarded mainnet deployment flows.
- Token-2022 metadata initialization.
- Exact 18,446,000,000 PWRC single genesis issuance.
- Freeze authority never enabled.
- Separate irreversible mint-authority finalization.
- Expected-mint guard for mainnet finalization.
- On-chain Token-2022 owner, decimals, supply, and authority verification.
- Transaction evidence capture and chain snapshots.
- Deterministic release hashes and release bundle.
- Version lock, metadata checks, u64 invariant tests, and CI validation.

### Production hardening

- Added qualified toolchain manifest (Agave 4.2.0 / SPL Token CLI 5.6.1).
- Added mainnet mint precommitment using an externally stored mint keypair.
- Added append-only SHA-256 chained deployment journal.
- Added deterministic input sealing before deployment.
- Added finalized transaction confirmation evidence checks.
- Added post-finalization journal verification.
- Expanded mainnet production checklist and interruption/recovery guidance.
- Release proof now commits to toolchain, input lock, journal and final transaction evidence.

### Client / Anchor integration

- Added `packages/sdk/src/client.ts` and `packages/sdk/src/index.ts`.
- Added `tests/anchor.test.ts`.
- Added pinned `@coral-xyz/anchor`, `@solana/web3.js`, `bs58`, and `axios` compatibility dependencies.
- Added finalized RPC mint verification and transaction evidence helpers.
- Added base58/JSON keypair decoding for local tooling without persisting secrets.

- Added typed PWRC/wPWRC bridge policy and 1:1 conservation verifier.
- Added Solana devnet/mainnet and Sui testnet/mainnet bridge identity templates.
- Added bridge client helper, bridge tests, bridge policy validator, and metadata/logo SHA-256 manifest.

- Changed canonical token website/domain to `https://token.powerchain.energy`.
- Changed primary PWRC/wPWRC metadata and logo URLs to `token.powerchain.energy`.
- Added configurable GitHub Raw secondary metadata fallbacks and Axios fallback client.
- Added metadata-source policy configuration, validation, tests, and refreshed SHA-256 manifest.
- Corrected official website to `https://powerchain.energy`; retained `https://token.powerchain.energy` exclusively as the token/metadata host.
- Added Anchor `programs/pwrc-fees` protocol-fee program with fixed 250 bps (2.5%) fee policy and 9-decimal PWRC validation.
- Added `config/fees.json`, TypeScript fee math, fee-config PDA and Solana instruction builders, program/fee tests, and `docs/protocol/PROGRAMS.md`.
- Added an optional Token-2022 `TransferFeeConfig` profile without enabling it on the canonical mint by default.
- Set canonical PWRC protocol fee collector owner to `FeeszhrKKEsvxr1kg8LDtPx6BLcEbYHiAThYaxajNhqy`; fee destination is its deterministic Token-2022 ATA for the canonical PWRC mint.
- Hardened protocol transactions with exact-byte simulation/submission, finalized blockhash-aware confirmation, receipt-based retry recovery, Token-2022 account preflight checks, immutable fee-vault configuration, source/destination separation, and canonical max-amount guards.
- Added secure AI-compute utility policy, x402 v2 integration policy, Circle CCTP v2 USDC settlement layer, and ZK/confidential-transfer governance policy. CCTP is explicitly separated from the PWRC/wPWRC bridge and canonical PWRC confidential transfers remain disabled by default.
- Added market/tradeability policy, universal zero-amount rejection, exact 9-decimal amount conversion, Pyth Hermes price adapter, Birdeye Solana price/V3 market-data adapter, and Pyth-to-Birdeye fallback market service.
- Added oracle confidence/divergence checks, liquidity and slippage gates, deterministic expiring trade quotes, verified multi-provider market snapshots, and a market circuit breaker.
- Added provider-health scoring, bounded read retries, hash-chained evidence envelopes, explicit production-readiness evaluation/reporting, and observability policy with secret-safe logging requirements.
- Replaced global zero-operation rejection with context-aware semantics: zero-value settlement is forbidden while signed messages, authentication, service, market-data, discovery, proof, simulation and other non-settlement operations may omit an amount or use zero. Added deterministic market/service IDs.
- Added Sui Move Currency-standard wPWRC package with zero genesis supply, immutable metadata, encapsulated TreasuryCap, capped bridge minting, burn return path, replay protection, emergency bridge pause, two-step authority rotation, TypeScript PTB builders, and testnet/mainnet deployment gates.
- Hardened wPWRC with fully bound Solana lock-claim hashes, pause-only authority governance, supply-after mint/burn events, OTW finalize-registration tooling, SHA-256 deployment manifests, transport-agnostic Sui deployment verification, and Solana-slot/Sui-checkpoint conservation evidence.
- Split wPWRC bridge operator and governor roles, added pause-gated two-step role rotation, monotonic mint/burn event sequences, expiring verifier authorization envelopes, explicit deployment-state transitions, and Testnet/Mainnet readiness gates.
- Added 2% quarterly canonical PWRC burn policy, immutable downward-only wPWRC canonical-supply ceiling, finalized burn evidence binding, and pinned Sui alias `powerchain` to `0x4a4a81c5e4a520c1b4d7b5b572a0567f48c6c7e85257f0a13e65639cfba49fb1`.
- Hardened 2% quarterly burns with on-chain quarter IDs, canonical burn-evidence replay protection, explicit cross-chain burn state machine, exact Solana/Sui reconciliation, and stale-relayer fail-closed mint guards.
- Added quarterly execution windows, independent Solana supply consensus, full-target/no-partial-burn enforcement, hash-chained burn journals, persisted burn-record schema, and fail-closed Mainnet burn readiness gates.
- Set the quarterly PWRC burn epoch to 2027 Q1 (`quarterId=20271`, sequence 1), reject pre-2027 burn quarters, require first burn at Q1 2027, add contiguous-quarter validation, and publish the 2027 burn calendar.
- Added repository-wide security model enforcement; canonical Token-2022 now requires TransferFeeConfig, MetadataPointer, and TokenMetadata; InterestBearingConfig/NonTransferable are forbidden; optional delegate/close/group extensions require governance+security evidence; System Program is explicitly rejected as a PowerChain deployment ID.
- Added canonical PowerChain website, documentation, whitepaper, X, and Telegram links to canonical, Metaplex-compatible, Token-2022, and wPWRC metadata payloads, with automated link validation.
- Hardened on-chain programs: fee-router now validates required TransferFeeConfig and prevents native/protocol double-fee stacking; added canonical Solana PWRC lock/release bridge program with receipt-PDA replay protection; Sui wPWRC now enforces contiguous quarterly burn IDs starting at 20271.
- Defined wPWRC as the 6-decimal Sui wrapped representation of 9-decimal canonical Solana PWRC; added exact 1000:1 base-unit conversion, non-representable amount rejection, unit-normalized 1:1 backing checks, separate metadata policy, and canonical metadata validation.
- Added @powerchain/native-token-client; fixed stale 9-decimal wPWRC client constants; enforced 6-decimal exact bridge conversion and zero-genesis bridge-only Sui issuance; added Sui balance/coin-account helpers, deployment config scripts, native Token-2022 2.5% fee/cap helpers, mint verification, Axios RPC client, and Solscan explorer profile.
- Upgraded bridge finality/reconciliation: explicit transfer state machine, strict Solana lock-receipt verification, Sui burn/release evidence helpers, unit-normalized backing reconciliation, deployment evidence schema, and idempotent retry policy.
- Added fail-closed bridge relayer architecture, deterministic idempotency records, bounded non-blind retries, observer/verifier/submitter role separation, cross-chain conservation watcher, deployment evidence commitments, and bridge release-readiness checks.
- Optimized runtime structure with shared bridge unit/conservation math, bounded relayer concurrency, in-flight read deduplication/cache, bounded Sui pagination, bounded Solana RPC batching, and centralized common error/invariant helpers.
- Added deterministic canonical serialization/evidence hashing, domain-separated replay keys, bounded relayer queue backpressure, strict runtime config parsing, bridge identity-bundle validation, and explicit mutable account-boundary checks.
- Added quarterly burn-intent race protection: Sui bridge pause and finalized on-chain burn intent must precede Solana burn submission; added intent commitments, cancellation rules, updated state sequencing, and release provenance evidence generation.
- Added persistent quarterly-burn execution records and SHA-256 chaining, strict Sui unpause evidence, bridge deployment-manifest validation, Mainnet readiness reporting, and stale wPWRC constant/maximum detection.

- Revised wPWRC canonical specification: 9 decimals on both Solana and Sui, true 1:1 base-unit accounting, zero wrapped genesis supply, bridge-only minting, no Token-2022 transfer fee, minimal MetadataPointer+TokenMetadata profile, and strict Sui alias/package-ID separation.
- Updated root Cargo/Anchor manifests for the revised no-fee canonical model: active workspace now contains only `pwrc-lock`; deprecated `pwrc-fees` is excluded; added workspace dependency pinning/release profiles, Sui testnet/mainnet/devnet/local RPC configuration, runtime network helpers, and refreshed root/program/docs READMEs.
- Updated TypeScript/package/script infrastructure: layered strict tsconfigs, pnpm workspace manifest, package-scoped native-token-client build/check scripts, root export/workspace validation, explicit no-transfer-fee check, active `pwrc-lock` program build/test commands, and split default vs optional Solana/Sui CI gates.
- Upgraded @powerchain/native-token-client into typed Solana/Sui/bridge modules, added stricter address/signature/coin-type validation, bounded RPC helpers, direct 1:1 bridge intent builders, stronger mint verification, and updated no-transfer-fee validation for the new package structure.
- Production/Mainnet hardening: removed remaining active 6-decimal/1000:1 and 2.5% logic; hardened `pwrc-lock`; fixed Sui burn logic and encapsulated `TreasuryCap<WPWRC>`; added bridge integration package, fail-closed Mainnet gates, immutable Sui revision requirement, current-model tests, and production provenance/docs.
- Production integration pass: corrected Sui transaction-builder module/argument targets, removed the duplicate legacy Sui package, encapsulated the wPWRC TreasuryCap, fixed the non-zero burn path, restored quarterly canonical-supply ceiling race protection in the active Sui package, added Token-2022 TransferFeeConfig rejection to pwrc-lock initialization, corrected pending-return conservation accounting to use nonnegative burned-but-not-yet-released exposure, added metadata-manifest verification, and consolidated 29 static production gates plus fail-closed Mainnet evidence/toolchain checks.
- Added `/idl/` with Anchor expected-interface drift manifest, generated-IDL schema/check/sync/hash tooling, Sui source-interface manifest, typed IDL exports, and strict separation between expected source contracts and real generated artifacts.
- Strengthened `/idl/` with Rust/Move source drift detection, complete Sui entry/event tracking, Anchor source-name/account mapping, domain-separated ABI fingerprints, strict generated-IDL verification, post-sync verification, and source-aware SHA-256 manifests.
- Hardened IDL release guarantees with immutable `1.0.0` compatibility baseline, breaking-change detection, generated Anchor discriminator validation, Sui normalized module verification, source/ABI/generated-artifact release manifest binding, and fail-closed IDL release readiness.
- Upgraded IDL/release integration with source-derived client bindings, ABI change classification, unsigned deterministic release attestations, Mainnet ABI fingerprint binding, and stricter policy that generated Anchor/Sui artifacts are required for runtime encoding/execution readiness.
- Further hardened IDL/release security with immutable baseline checksum, Anchor source/Anchor.toml program-ID consistency check, runtime ABI guards, unsigned attestation verification, release requirement for both generated Anchor IDL and normalized Sui modules, explicit IDL readiness reporting, and Mainnet preflight binding to `release-idl-ready`.
- Added `programs/token/src/` as a dedicated canonical PWRC Token-2022 verification package with fixed-supply/authority/extension invariants, no public mint surface, Cargo/Anchor integration, expected IDL contract, static validation, tests, and production documentation.
- Canonical PWRC transfer-fee policy updated before release readiness: TransferFeeConfig is now required at 250 bps (2.5%) with a 1,000,000 PWRC maximum; canonical mint set to PWRCRXXZxbg6FdQZfK3PMD7KP8xfxs9acvifJiG46wc; metadata URI set to https://powerchain.energy/metadata/metaplex.json; token verifier, bridge accounting, native client, configs, IDL policy, tests, Mainnet gates, and READMEs updated consistently while version remains 1.0.0.
- Added production PWRC/wPWRC PNG assets under `public/assets/`, expanded canonical metadata for utility/governance/RWA/tokenized-renewables positioning, added website/bridge/app links, normalized metadata JSON, corrected wPWRC backing text to the 9-decimal fee-aware model, and kept all metadata at version 1.0.0.
- Upgraded scripts, contracts, runtime utilities, configs, constants, transaction handlers, replay/idempotency logic, tests, docs, Devnet/Mainnet readiness and optional Next.js host configuration; removed active custom fee-router assumptions while keeping version 1.0.0.
- Updated JavaScript production toolchain to Node 26.7.0 Current and pnpm 10.21.0; explicitly approved bigint-buffer@1.1.5, bufferutil@4.1.0, esbuild@0.25.12 and utf-8-validate@6.0.6 through pnpm 10 `onlyBuiltDependencies`, enabled strict dependency-build failure policy, disabled Next/Turbo telemetry, updated CI/toolchain checks/cache cleanup/docs, and kept every project package at version 1.0.0.
- Fixed macOS doctor portability by replacing the hard GNU `sha256sum` requirement with `sha256sum`/`shasum`/OpenSSL fallback logic; fixed strict TypeScript/NodeNext compiler errors in env access, canonical mint exports, burn quarter typing, Error.cause handling, exact optional retry options, JSON import attributes, fee-aware client tests and deployment manifest tests; added regression gates and kept version 1.0.0.
- Hardened runtime utilities with typed environment parsing, bounded retry attempts/delays, safe request IDs, finalized ambiguous-write recovery without resubmission, stricter runtime-policy relationships, workspace-aware cache cleanup, new runtime tests and production regression checks; version remains 1.0.0.
- Added durable file-backed relayer idempotency/replay stores with restart rehydration, atomic JSON/file writes, bounded transaction reconciliation deadlines, stricter URL validation, deterministic self-hashed release provenance verification, and restart/replay regression tests; version remains 1.0.0.
- Added fail-closed Mainnet release phases, strict Solana/Sui deployment-evidence validation, signed evidence template, deterministic build/provenance/ABI commitments, Mainnet evidence preparation/verification commands and release documentation; code can be Mainnet-ready by design without fabricating deployment evidence. Version remains 1.0.0.
- Strengthened Mainnet release authorization with real Ed25519 evidence-signature verification, deterministic build-artifact manifests, required Anchor program `.so` hashes, build-manifest verification in `buildReady`, and build-manifest commitments inside signed deployment evidence; version remains 1.0.0.
- Added dual-provider Solana/Sui Mainnet observation evidence and a separate `releaseAuthorized` phase with short-lived Ed25519 authorization bound to the exact deployment evidence, provenance, build manifest, canonical mint, networks, nonce and expiry; release authorization is now distinct from deployment verification. Version remains 1.0.0.
- Added Mainnet evidence-to-artifact hash binding, deterministic public signing-payload export, one-time release-authorization nonce consumption with atomic exclusive ledger writes, unused-authorization preflight checks, and normalized blocker reporting; version remains 1.0.0.
- Added a forward-only Mainnet release state machine, fresh preflight proof binding, five-minute authorization clock-skew policy, consumption-time nonce/preflight checks, and stronger build-manifest/ABI/IDL cross-validation; version remains 1.0.0.
- Reorganized the full root utility architecture: added canonical dependency-free `packages/runtime/src/` modules for atomic writes, canonical JSON, SHA-256, paths, time, network validation, redaction and validation; added typed `packages/protocol/src/utils/` public re-exports, migrated Mainnet/release tooling onto shared helpers, added root architecture checks and documentation; version remains 1.0.0.
- Expanded the full root platform utility layer with centralized config registry/loading, canonical token constants, environment parsing, safe shell-free process execution, structured redacted logging, error serialization, duplicate-helper detection, and migrated the production checker onto shared atomic/process primitives; version remains 1.0.0.
- Security/bug-fix pass: strict canonical JSON for signed/hash payloads, embedded credential redaction, bounded shell-free process execution, repository-contained/symlink-safe config reads, randomized atomic temp files, shared Mainnet preflight process execution, root security regression tests, and refreshed README/production/Mainnet/security documentation. Version remains 1.0.0.
- Added a fully wired `apps/api` + `apps/client` stack with health/readiness/token/Mainnet status endpoints, deterministic Token-2022 bridge quotes, same-origin web API proxy, server-only authenticated/idempotent execution adapter, bounded request/proxy limits, OpenAPI contract, application configuration, and updated API/root README documentation; version remains 1.0.0.
- Hardened the wired full stack with bounded read/write rate limiting, two-second Mainnet status caching with fresh execution checks, durable file-backed execution idempotency, chain-specific destination validation, required quote fingerprints, authenticated execution status/reconciliation, ambiguity-safe executor semantics, process metrics, a full-stack launcher, runtime regression tests and updated OpenAPI/API/README documentation; version remains 1.0.0.
- Reworked JavaScript toolchain compatibility after macOS dyld/libc++ failures: local baseline is Node 22.22.3, modern CI also qualifies Node 24.18.1, Node 26.5.1 remains supported on compatible hosts, removed pnpm useNodeVersion/nodeVersion auto-download pins, added native macOS preflight/recovery scripts, broadened engine policy to supported 22/24/26 lanes, and updated toolchain checks/docs while preserving pnpm 10.21.0 and product version 1.0.0.
- Fixed the macOS bootstrap persistence bug: Node activation is now a sourceable shell workflow so nvm/PATH changes remain active in the user's parent zsh session; preflight detects the known broken Node 26.7.0 path before executing it, shell inspection detects hard-coded startup PATH entries, Corepack/pnpm are reactivated only after Node 22.22.3 is verified, and recovery docs now explicitly distinguish executing from sourcing bootstrap scripts. Version remains 1.0.0.
- Fixed validation drift found on a clean Node 22.22.3/pnpm 10.21.0 install: added typed declarations for Mainnet release-state ESM, replaced obsolete operation test class `read` with `status`, aligned RPC HTTPS error expectations with the shared URL hardening layer, corrected quarterly-burn wrapped-ceiling and undercollateralized relayer fixtures, restored TransferFeeConfig in canonical extension assertions, aligned wPWRC metadata name and official-link tests with current canonical configuration, and made telemetry/bootstrap production checks portable across clean checkouts. Version remains 1.0.0.
- Hardened full-stack startup against EADDRINUSE: the supervisor now checks API/web ports before spawning, starts API and waits for health before web, reports the listening process when available, supports opt-in automatic free-port fallback via `fullstack:start:auto`, wires the web proxy to the selected API port, handles server listen errors structurally, and performs coordinated shutdown. Added collision/fallback regression tests and documentation. Version remains 1.0.0.
- Removed the final environment-dependent production-check false positives: platform-bootstrap and telemetry checks now validate tracked repository policy rather than developer-local VS Code NODE_OPTIONS or `.env.production`; activation clears known VS Code Auto Attach injection before Node/Corepack; added a portability regression check and root `start`/`start:auto` aliases. Version remains 1.0.0.
- Added standalone `@powerchain/docs` application under `apps/docs`, reusable documentation rendering components under `components/docs`, and structured technical content sessions under `sessions`. Added responsive light/dark technical-docs UI, route-based sessions, search/navigation, health and session metadata endpoints, docs-app tests and production checks. Version remains 1.0.0.
- Renamed the runnable browser workspace from `apps/web` / `@powerchain/web` to `apps/client` / `@powerchain/client`, while preserving reusable client libraries in `packages/sdk` and `packages/native-token-client`. Added GitHub repository naming policy recommending `powerchain-token`, registered repository metadata policy, migrated client runtime environment variables and full-stack validation. Version remains 1.0.0.
- Set canonical GitHub repository metadata to `powerchain-network/powerchain-token` across root package metadata, repository policy, README and technical documentation. Version remains 1.0.0.
- Set the canonical GitHub repository to `powerchain-network/powerchain-token` and hardened npm dependency resolution: replaced vulnerable `bigint-buffer` with a local pure-JS compatibility package for CVE-2025-3194, removed its native install approval, pinned affected `uuid` ranges to official patched releases for CVE-2026-41907, and added static/installed-tree security verification. Version remains 1.0.0.
