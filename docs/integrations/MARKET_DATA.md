# Market Data

Market-data providers are non-authoritative read sources. Prices, pools, liquidity, and quotes must never determine canonical PWRC/wPWRC identity or bridge finality.

Canonical PWRC transfers do not include a Token-2022 transfer fee. Quote/risk layers should model only explicit application/service/network costs that actually apply to the requested operation.
