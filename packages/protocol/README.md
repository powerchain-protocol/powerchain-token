# @powerchain/protocol

Canonical PowerChain `1.0.0` protocol policy and deterministic domain logic.

This package owns token constants, Token-2022 policy, bridge conservation,
burn policy, runtime operation policy, security guards, metadata policy,
relayer state and Mainnet readiness types.

It contains **source policy**, not deployment evidence.
