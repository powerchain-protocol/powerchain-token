# @powerchain/runtime

Shared Node.js runtime utilities used by PowerChain applications and release
tooling: deterministic JSON, hashing, environment parsing, paths, process
execution, logging, redaction, network validation and atomic JSON writes.
