# @powerchain/sdk

PowerChain `1.0.0` high-level client SDK for Solana, Sui, bridge, market,
metadata, transaction and burn workflows.

The SDK does not embed private keys or server-side bridge executor secrets.
