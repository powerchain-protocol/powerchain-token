packages:
  - "apps/*"
  - "packages/*"

onlyBuiltDependencies:
  - "bufferutil@4.1.0"
  - "esbuild@0.28.1"

ignoredBuiltDependencies:
  - "utf-8-validate"
  - "bigint-buffer"

# Dependency hygiene
# uuid 8.3.2 is deprecated. Root package.json overrides only that
# transitive release to the maintained CommonJS-compatible uuid 11 line.

# Canonical package manager: pnpm 11.18.0.
# Node 26 does not bundle Corepack; install pnpm explicitly or use pnpm's
# standalone installer. Never generate a lockfile with a different major.
