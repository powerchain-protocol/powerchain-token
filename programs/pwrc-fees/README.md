# `pwrc-fees` — deprecated

This directory is retained for historical compatibility only.

Canonical PowerChain `1.0.0` does **not** use Token-2022 `TransferFeeConfig`
and does not use a protocol transfer-fee router.

This program is excluded from the root Cargo workspace and Anchor program map.
Do not deploy it as part of the canonical PWRC release.
