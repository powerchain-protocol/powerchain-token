import fs from "node:fs"; const failures=[]; const a=fs.readFileSync("src/bridge/receipt.ts","utf8"); const b=fs.readFileSync("src/bridge/reconcile.ts","utf8"); if(!a.includes("receipt.amountBaseUnits !== receipt.wrappedAmountBaseUnits"))failures.push("receipt:1-to-1-check"); if(b.includes("* 1_000n")||b.includes("/ 1_000n"))failures.push("reconcile:stale-conversion"); console.log(JSON.stringify({ok:!failures.length,version:"1.0.0",upgrade:"9-decimal-1-to-1",failures},null,2)); if(failures.length)process.exit(1);
